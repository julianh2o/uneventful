# Production Docker Compose Configuration
# Uses pre-built image from Docker Hub
# This file is standalone - you can copy it anywhere and run it

version: '3.8'

services:
  uneventful:
    image: julianh2o/uneventful:latest
    container_name: uneventful-prod
    ports:
      - "2999:2999"
    environment:
      - PORT=2999
      # REQUIRED: Set these in a .env file
      - TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID}
      - TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN}
      - TWILIO_PHONE_NUMBER=${TWILIO_PHONE_NUMBER}
      - JWT_SECRET=${JWT_SECRET}
      - APP_BASE_URL=${APP_BASE_URL}
      - NODE_ENV=production
    volumes:
      # Persist data files
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:2999/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
